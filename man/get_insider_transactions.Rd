% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/insider.R
\name{get_insider_transactions}
\alias{get_insider_transactions}
\title{Retrieves insider transactions}
\usage{
get_insider_transactions(
  ticker = NULL,
  exchange = "US",
  first_date = Sys.Date() - 90,
  last_date = Sys.Date(),
  limit = 100,
  cache_folder = get_default_cache(),
  check_quota = TRUE
)
}
\arguments{
\item{ticker}{an optional company ticker (e.g. "AAPL"). Default (NULL) returns
the transactions of every company in the window.}

\item{exchange}{the exchange of the ticker (default "US")}

\item{first_date}{the first date of the window. Default is 90 days ago.}

\item{last_date}{the last date of the window. Default is today.}

\item{limit}{maximum number of rows (default 100, maximum 1000)}

\item{cache_folder}{A local directory to store cache files. By default, all functions use a temporary path, meaning that the caching system
is session persistent (it will remove all files when you exit your R session). If you want a persistent caching system, simply point argument
cache_folder to a local directory in your filesystem. Be aware, however, that a persistent cache will not refresh your data for new api queries.}

\item{check_quota}{A flag (TRUE/FALSE) for whether to check the current quota status from the api. This option implies a small cost of execution
time. If you need speed, just set it to FALSE.}
}
\value{
A dataframe with the insider transactions
}
\description{
Queries \url{https://eodhd.com/financial-apis/insider-transactions-api}, with the
purchases and sales reported by the insiders of US companies (SEC form 4).
}
\examples{
\dontrun{
set_token("YOUR_VALID_TOKEN")
df_insider <- get_insider_transactions("AAPL")
}
}
