% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{get_options_contracts}
\alias{get_options_contracts}
\title{Retrieves option contracts}
\usage{
get_options_contracts(
  underlying_symbol = "AAPL",
  contract = NULL,
  type = NULL,
  exp_date = NULL,
  first_exp_date = NULL,
  last_exp_date = NULL,
  first_strike = NULL,
  last_strike = NULL,
  sort = NULL,
  limit = 100,
  offset = 0,
  cache_folder = get_default_cache(),
  check_quota = TRUE
)
}
\arguments{
\item{underlying_symbol}{the underlying symbol, without the exchange
(e.g. "AAPL"). This endpoint only covers US options, so a symbol given with
its exchange ("AAPL.US") is trimmed for you.}

\item{contract}{an optional single contract code (e.g. "AAPL270115C00200000").
When given, the underlying symbol is ignored by the api.}

\item{type}{an optional type of contract ("call" or "put")}

\item{exp_date}{a single expiration date, or NULL}

\item{first_exp_date}{an optional first expiration date of the window}

\item{last_exp_date}{an optional last expiration date of the window}

\item{first_strike}{an optional lower bound of the strike}

\item{last_strike}{an optional upper bound of the strike}

\item{sort}{an optional sort expression (e.g. "-exp_date")}

\item{limit}{maximum number of rows (default 100, maximum 1000)}

\item{offset}{pagination offset (default 0)}

\item{cache_folder}{A local directory to store cache files. By default, all functions use a temporary path, meaning that the caching system
is session persistent (it will remove all files when you exit your R session). If you want a persistent caching system, simply point argument
cache_folder to a local directory in your filesystem. Be aware, however, that a persistent cache will not refresh your data for new api queries.}

\item{check_quota}{A flag (TRUE/FALSE) for whether to check the current quota status from the api. This option implies a small cost of execution
time. If you need speed, just set it to FALSE.}
}
\value{
A dataframe with the option contracts
}
\description{
Queries the contracts of the Unicorn Bay options data
\url{https://eodhd.com/marketplace/unicornbay/options}. Each row is one
contract, with its strike, expiration, greeks and implied volatility.
}
\examples{
\dontrun{
set_token("YOUR_VALID_TOKEN")
df_contracts <- get_options_contracts("AAPL", type = "call")
}
}
